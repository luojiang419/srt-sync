# 进度快照 52 - 合板详情自动锚点循环核对版本发布完成

## 版本信息
- 新版本号: `v1.1.4`
- `pubspec.yaml`: `1.1.4+22`
- 阶段备份: `backup/v0.30.0`
- 发布目录: `dist/v1.1.4`

## 已完成内容

### 1. 合板详情与复核弹窗已发布自动锚点循环核对能力
- 顶部已支持 `合板锚点`
- 可按顺序循环查看一键合板使用的自动锚点
- 左右字幕会同步跳转到对应锚点处
- 预览区会同步刷新

### 2. 自动锚点与整轨总字幕的映射能力已接入
- 自动合板锚点不再只能看当前音频局部字幕
- 现在可直接映射到右侧整轨总字幕视图核对
- 手动匹配、接受、移除、恢复等原有流程保持不变

### 3. 构建与启动冒烟已完成
- `flutter build windows --release`
  - 通过
- 已生成：
  - `build/windows/x64/runner/Release/asr_tools.exe`
  - `dist/v1.1.4`
- 已完成启动冒烟：
  - `dist/v1.1.4/asr_tools.exe`
  - 可正常拉起并完成关闭验证

## 验证结果

### 1. 静态检查
- `dart analyze lib test`
  - 通过
  - 无 error

### 2. 测试
- `flutter test --no-pub`
  - 通过

### 3. Windows 构建
- `flutter build windows --release`
  - 通过

## 当前修改到哪个模块
- 自动锚点解析与排序
- 合板详情与复核弹窗顶部锚点按钮
- 锚点循环跳转逻辑
- 组件测试与单元测试
- 版本构建与发布

## 待办清单
- [ ] 可继续做真实项目里的多锚点循环核对验证
- [ ] 如需要，可增加显式锚点列表或上一条 / 下一条控件
- [ ] 如需要，可继续联动时间线定位

## 下一步
- 可以直接使用 `dist/v1.1.4` 在真实项目里核对一键合板的自动锚点与整轨字幕映射效果。
