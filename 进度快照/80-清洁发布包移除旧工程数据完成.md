# 进度快照 80 - 清洁发布包移除旧工程数据完成

## 已完成内容

### 1. 已定位旧工程卡片混入发布包的原因
- 新编译软件里出现旧工程卡片，不是软件自带预置工程
- 根因是：
  - 发布版首次启动冒烟时
  - `AppDataService` 检测到发布目录缺少 `data/database/asr_tools.db`
  - 自动把旧环境数据库迁移到了 `dist` 目录
- 因此 `dist` 被污染后，后续直接打开该发布目录，就会看到旧工程卡片

### 2. 已新增清洁发布脚本
- 新增：
  - `tool/prepare_clean_release.py`
- 脚本会对指定发布目录执行：
  - 重建 `data/config`
  - 重建 `data/database`
  - 重建 `data/projects`
  - 重建 `data/temp`
  - 写入空的 `asr_tools_settings.json`
  - 依据 `database_service.dart` 当前建表 SQL 初始化一份全新空数据库
- 初始化后的数据库：
  - 表结构完整
  - `projects` 表为空
  - 不再触发旧环境数据库迁移

### 3. 已验证清洁发布脚本有效
- 对 `dist/v1.1.16` 执行清洁脚本后：
  - `projects` 表为空
  - 配置文件为默认空配置
- 在临时副本中启动验证后：
  - 数据目录正常生成
  - `projects` 表仍为空
  - 不会再把旧工程卡片迁回

## 当前修改到哪个模块
- 发布目录清洁脚本
- 清洁发布流程验证

## 验证结果
- `python3 tool/prepare_clean_release.py dist/v1.1.16`
  - 通过
- 清洁后数据库查询：
  - `projects` 表为空
- 临时副本启动冒烟后再次查询：
  - `projects` 表仍为空

## 待办清单
- [ ] 递增版本号并重新输出一份干净的正式发布包
- [ ] 补版本发布快照
- [ ] 提交并推送本次发布清洁流程修复

## 下一步
- 基于这套清洁发布流程重新生成一个真正干净的 `dist` 版本目录，直接给你可用的新软件。
