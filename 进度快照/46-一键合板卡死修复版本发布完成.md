# 进度快照 46 - 一键合板卡死修复版本发布完成

## 版本信息
- 新版本号: `v1.1.1`
- `pubspec.yaml`: `1.1.1+19`
- 阶段备份: `backup/v0.27.0`
- 发布目录: `dist/v1.1.1`

## 已完成内容

### 1. 一键合板卡死修复已落地
- 重 CPU 匹配过程已迁移到 isolate worker
- UI 主线程不再直接执行全量字幕比较
- 已支持：
  - 阶段进度显示
  - 当前视频显示
  - 处理中遮罩
  - 取消合板

### 2. 性能止血措施已落地
- 已增加窗口预筛：
  - 同窗口尺寸比较
  - 文本长度差比例过滤
  - 无 bigram 交集过滤
  - Dice 预分过滤
- 已增加 bucket 限流：
  - 每个 `videoWindow -> audioFile` 最多 8 条
  - 每个 `audioFile` 总命中最多 48 条
- 已将锚点搜索改为：
  - 优先围绕 `fallbackOffsetMs` 局部搜索
  - 无结果再回退全列表

### 3. 高频短句降权已落地
- 已对低价值短句窗口做权重压制：
  - `嗯`
  - `啊`
  - `哦`
  - `好`
  - `行`
  - `操`
  - `我操`
  - `可以`
  - `开始`
  - `等一下`

### 4. 真实素材基准已通过
- 基准路径：
  - `G:\data\260224-元数据脚本测试\1_Video\220822shipin`
  - `G:\data\260224-元数据脚本测试\2_Audio\220822yinpin`
  - `G:\data\260224-元数据脚本测试\3_srt`
- 实测结果：
  - 导入：约 `2.32s`
  - 字幕准备：约 `0.52s`
  - 一键合板：约 `4.11s`
  - 时间线构建：约 `0.01s`
  - 总耗时：约 `7.08s`
- 已导出：
  - `测试合板/220822_real_sync_benchmark.xml`
  - `测试合板/220822_real_sync_benchmark.fcpxml`
  - `测试合板/220822_real_sync_benchmark.csv`

### 5. 辅助脚本与测试已同步
- 新增：
  - `tool/run_real_subtitle_sync_benchmark.dart`
  - `test/subtitle_match_service_test.dart`
  - `test/subtitle_prepare_service_test.dart`
- 已同步更新旧工具脚本接口：
  - `tool/sample_workflow.dart`
  - `tool/run_sample_workflow.dart`

### 6. 数据库升级健壮性已修复
- 修复 `DatabaseService._createIndexes()` 非幂等问题
- 老数据库重复打开时不再因索引已存在而初始化失败

## 验证结果

### 1. 静态检查
- `dart analyze lib test tool`
  - 通过
  - 无 error
  - 仍保留项目历史 info 提示

### 2. 测试
- `flutter test --no-pub`
  - 通过

### 3. Windows 构建
- `flutter build windows --release`
  - 通过
- 已生成：
  - `build/windows/x64/runner/Release/asr_tools.exe`
  - `dist/v1.1.1`

### 4. 启动冒烟
- `dist/v1.1.1/asr_tools.exe`
  - 启动检查通过
  - 1 秒内进程正常拉起
  - 已完成关闭验证

## 当前修改到哪个模块
- `lib/services/subtitle_match_service.dart`
- `lib/providers/match_provider.dart`
- `lib/widgets/step_match.dart`
- `lib/services/subtitle_prepare_service.dart`
- `lib/services/database_service.dart`
- `test/*`
- `tool/*`
- `pubspec.yaml`
- `lib/screens/settings_screen.dart`

## 待办清单
- [ ] 如仍有个别素材批次匹配偏慢，再做下一轮“两阶段召回 / 真正索引化”
- [ ] 如需要，可继续对 220822 导出的 XML 在 PR / 达芬奇里做人工导入验证
- [ ] 如需要，可继续调低价值短句表和预筛阈值

## 下一步
- 可以直接使用 `dist/v1.1.1` 对真实素材继续验证。
- 下一轮如果继续优化，优先看误匹配质量和更大批次下的稳定耗时。
