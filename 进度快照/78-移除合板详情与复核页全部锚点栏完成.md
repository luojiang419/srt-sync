# 进度快照 78 - 移除合板详情与复核页全部锚点栏完成

## 已完成内容

### 1. 合板详情与复核页底部“全部锚点”栏已移除
- `SyncReviewPage` 底部的 `全部锚点` 列表区已删除
- 不再显示：
  - `全部锚点` 标题
  - 底部锚点列表
  - 逐条锚点的相似度 / 偏移 / 文本预览

### 2. 复核主流程保留
- 上方 `合板锚点 1/N` 循环跳转保留
- 关键词搜索、命中列表、返回原文、手动匹配预览保留
- `匹配预览` 区域保留，但高度已收回，不再为底部锚点列表预留空间
- 已选视频字幕 / 已选音频总字幕 / 预览结果三栏保持可用

### 3. 相关遗留代码已同步清理
- `_ResolvedAnchorPreviewItem` 已删除
- `_buildAnchorPreviewList()` 已删除
- `_buildResolvedAnchorPreviewItems()` 已删除
- 已清理因该列表删除而变成未使用的辅助方法

## 当前修改到哪个模块
- `sync_review_dialog.dart` 复核页预览布局
- `sync_review_dialog_test.dart` 相关断言更新

## 验证结果
- `flutter test --no-pub test/sync_review_dialog_test.dart test/sync_result_review_test.dart`
  - 通过
- `flutter test --no-pub`
  - 通过
- `flutter analyze lib test`
  - 无新增 error / warning
  - 仍存在仓库既有 info 级提示

## 待办清单
- [ ] 递增版本号并构建新的 Windows release
- [ ] 复制发布产物到新的 `dist` 目录
- [ ] 做启动冒烟并补发布快照

## 下一步
- 进入版本发布流程，生成新版本目录供你直接验证复核页底部“全部锚点”栏是否已完全消失。
