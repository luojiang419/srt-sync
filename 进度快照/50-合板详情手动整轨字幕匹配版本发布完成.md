# 进度快照 50 - 合板详情手动整轨字幕匹配版本发布完成

## 版本信息
- 新版本号: `v1.1.3`
- `pubspec.yaml`: `1.1.3+21`
- 阶段备份: `backup/v0.29.0`
- 发布目录: `dist/v1.1.3`

## 已完成内容

### 1. 手动整轨字幕合板功能已发布
- 合板详情与复核弹窗已支持：
  - 整轨音频总字幕展示
  - 搜索左右全部字幕
  - 手动点击左右字幕条
  - 预览目标音频与裁切区间
  - 点击 `匹配` 保存手动合板结果

### 2. 手动匹配结果已接入后续流程
- 匹配结果会写回：
  - `audioFileId`
  - `audioSourceInMs`
  - `audioSourceOutMs`
  - `status`
  - `method = manual`
  - `reviewStatus = accepted`
- 时间线构建继续复用现有 `AudioAlignService`

### 3. 构建与启动冒烟已完成
- `flutter build windows --release`
  - 通过
- 已生成：
  - `build/windows/x64/runner/Release/asr_tools.exe`
  - `dist/v1.1.3`
- 已完成启动冒烟：
  - `dist/v1.1.3/asr_tools.exe`
  - 可正常拉起并完成关闭验证

## 验证结果

### 1. 静态检查
- `dart analyze lib test`
  - 通过
  - 无 error

### 2. 测试
- `flutter test --no-pub`
  - 通过

### 3. Windows 构建
- `flutter build windows --release`
  - 通过

## 当前修改到哪个模块
- 复核详情数据聚合
- 手动锚点匹配服务
- 合板详情与复核弹窗
- Step 3 手动匹配入口文案
- 自动化测试
- 版本构建与发布

## 待办清单
- [ ] 用真实项目继续验证手动整轨字幕匹配后的导出结果
- [ ] 如需要，可增加批量人工匹配工作流
- [ ] 如需要，可继续做多总字幕文件支持

## 下一步
- 可以直接使用 `dist/v1.1.3` 在真实项目里执行手动合板复核与导出验证。
