# 进度快照 81 - 清洁发布包移除旧工程数据版本发布完成

## 版本信息
- 新版本号: `v1.1.17`
- `pubspec.yaml`: `1.1.17+35`
- 阶段备份: `backup/v0.41.0`
- 发布目录: `dist/v1.1.17`

## 已完成内容

### 1. 已输出真正干净的发布包
- 新发布包已按清洁流程生成：
  - `dist/v1.1.17`
- 该目录中的：
  - `data/config/asr_tools_settings.json`
  - `data/database/asr_tools.db`
  - `data/projects`
  - `data/temp`
  都已重新初始化
- `projects` 表为空，不再包含任何旧工程卡片

### 2. 已补齐“清洁发布”流程能力
- 新增：
  - `tool/prepare_clean_release.py`
- 用途：
  - 在发布版目录中自动清空旧数据
  - 建立完整但空的数据库
  - 阻止旧环境数据库迁移污染正式包

### 3. 已验证清洁包首启后仍然干净
- 正式包 `dist/v1.1.17`：
  - 数据库 `projects` 表为空
- 以临时副本做启动冒烟后：
  - 软件可正常拉起
  - 数据目录正常工作
  - `projects` 表仍为空
- 这样正式交付目录不会因为启动验证而再被污染

### 4. 构建与校验已完成
- `flutter build windows --release`
  - 通过
- 已生成：
  - `build/windows/x64/runner/Release/asr_tools.exe`
  - `dist/v1.1.17`
- 已完成临时副本启动冒烟：
  - 可正常拉起并自动关闭
- 已确认正式发布目录存在：
  - `dist/v1.1.17/data/config/asr_tools_settings.json`
  - `dist/v1.1.17/data/database/asr_tools.db`
  - `dist/v1.1.17/data/projects`
  - `dist/v1.1.17/data/temp`

## 当前修改到哪个模块
- 发布目录清洁脚本
- 干净数据库初始化流程
- 干净发布包输出与验证

## 待办清单
- [ ] 如需要，可继续把清洁发布步骤固化成完整的一键发布脚本
- [ ] 如需要，可继续在操作手册里补充“正式包为何不能直接用冒烟后的 data”说明

## 下一步
- 可以直接使用 `dist/v1.1.17`。
- 这版应该不会再出现旧工程卡片；如果你打开后仍看到工程卡片，我会继续追到具体来源并彻底清掉。
