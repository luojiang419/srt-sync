# 进度快照 79 - 移除合板详情与复核页全部锚点栏版本发布完成

## 版本信息
- 新版本号: `v1.1.16`
- `pubspec.yaml`: `1.1.16+34`
- 阶段备份: `backup/v0.41.0`
- 发布目录: `dist/v1.1.16`

## 已完成内容

### 1. 复核页底部“全部锚点”栏已完全移除
- `合板详情与复核` 页面底部不再显示 `全部锚点`
- 对应的整块锚点预览列表已删除
- 页面不再使用这套底部锚点栏

### 2. 复核页主要操作能力保持不变
- 上方 `合板锚点 1/N` 循环跳转保留
- 关键词搜索与命中列表保留
- 返回原文、手动匹配预览、匹配按钮逻辑保留
- `匹配预览` 区域已压缩回仅显示三栏核心内容

### 3. 测试、构建与启动冒烟已完成
- `flutter test --no-pub test/sync_review_dialog_test.dart test/sync_result_review_test.dart`
  - 通过
- `flutter test --no-pub`
  - 通过
- `flutter analyze lib test`
  - 无新增 error / warning
  - 仍存在仓库既有 info 级提示
- `flutter build windows --release`
  - 通过
- 已生成：
  - `build/windows/x64/runner/Release/asr_tools.exe`
  - `dist/v1.1.16`
- 已完成启动冒烟：
  - `dist/v1.1.16/asr_tools.exe`
  - 正常拉起并完成自动关闭验证
- 已确认存在：
  - `dist/v1.1.16/data/config/asr_tools_settings.json`
  - `dist/v1.1.16/data/database/asr_tools.db`
  - `dist/v1.1.16/data/projects`
  - `dist/v1.1.16/data/temp`

## 当前修改到哪个模块
- 复核页底部锚点列表移除
- 复核页匹配预览区收高
- 复核页相关测试
- 版本构建与发布

## 待办清单
- [ ] 如需要，可继续压缩复核页顶部或中部的其他说明占位
- [ ] 如需要，可继续把锚点循环切换入口做得更显眼一些
- [ ] 如需要，可继续评估是否同步精简其他不常用的复核辅助区块

## 下一步
- 可以直接使用 `dist/v1.1.16` 验证：
  - 合板详情与复核页底部 `全部锚点` 是否已消失
  - 页面高度是否回收给上方主要复核内容
  - 上方 `合板锚点 1/N` 循环跳转是否仍然正常
