# 进度快照 38 - 启动窗口不可见修复

## 版本信息
- 版本号: v1.0.13
- 备份: backup/v0.21.0
- 编译: dist/v1.0.13

## 已完成内容

### 1. 已定位真实问题
- 软件启动后进程存在，但窗口没有正常显示
- 实测不是程序未启动，也不是崩溃退出
- 通过窗口句柄读取到的窗口矩形长期停留在：
  - `Rect=-32000,-32000,-31840,-31961`
- 这属于 Windows 常见的隐藏/不可见窗口占位状态

### 2. 已为启动流程补“窗口可见性兜底”
- 保留原有 `waitUntilReadyToShow` 逻辑
- 额外新增启动后的可见性探测与修复：
  - 延迟探测窗口位置与尺寸
  - 如果发现窗口处于 `-32000,-32000` 这类隐藏状态
  - 自动将窗口恢复到可视区域
  - 当前默认恢复到 `120,80`

### 3. 已补窗口几何合法性校验
- 启动时不再盲目信任历史窗口位置
- 只有当保存的窗口位置/尺寸满足合理条件时才恢复
- 异常窗口尺寸或异常坐标会自动回退到默认窗口参数

### 4. 修复后会自动回写新的窗口配置
- 当窗口被拉回桌面后，会把新位置重新写回设置文件
- 当前设置文件位置已确认：
  - `C:\Users\jiang\AppData\Roaming\com.asrtools\asr_tools\asr_tools_settings.json`
- 当前写回后的窗口参数为：
  - `window_x = 120`
  - `window_y = 80`
  - `window_width = 1280`
  - `window_height = 800`

### 5. 版本与发布产物已更新
- `pubspec.yaml` 已更新为 `1.0.13+13`
- 设置页关于信息已更新为 `v1.0.13`
- 已完成 Windows release 构建并复制到：
  - `dist/v1.0.13`

## 验证结果

### 1. 静态检查
- `flutter analyze --no-pub --no-fatal-infos`
  - 通过
  - 无新增 error / warning
  - 当前仍保留 35 条项目历史 info 提示

### 2. 测试
- `flutter test --no-pub`
  - 通过

### 3. 启动可见性验证
- 修复前：
  - 窗口矩形稳定停在 `-32000,-32000`
- 修复后：
  - `t=3 Rect=120,80,1400,880`
  - `t=8 Rect=120,80,1400,880`
  - `t=15 Rect=120,80,1400,880`
- 说明窗口已稳定恢复到桌面可视区域

## 修改的文件
- `pubspec.yaml`
- `lib/main.dart`
- `lib/screens/settings_screen.dart`

## 当前状态
- 本轮问题已修复。
- 当前新版本已能够在启动后把隐藏窗口拉回可见区域。

## 待办清单
- [ ] 如有需要，可继续把“窗口位置是否仍在当前显示器可视范围内”做成更严格的多显示器校验
- [ ] 如有需要，可在设置页增加“重置窗口位置”按钮
- [ ] 如有需要，可继续清理项目历史遗留的 info 级静态提示

## 下一步
- 可直接使用 `dist/v1.0.13` 版本继续验证，如果还出现窗口异常，再继续收窄到多屏幕边界恢复逻辑。
