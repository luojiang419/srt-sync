我现在要处理一个大型、多轮、长上下文任务，请你严格遵守以下规则：
全程使用中文和我交流

## 一、任务模式
1. 先做整体规划，不直接开始写完整内容。
2. 输出清晰的：任务目标、拆分步骤、文件/模块清单、待办清单、验收标准。
3. 每次只完成一小部分，完成后告诉我当前进度，不要一次性输出超长内容。
4. 使用模块化功能结构开发，易于维护，开发新功能模块后需要全局检测是否整体正常运行。

## 二、进度快照
- 每完成一个功能的修改就自动生成一份「进度快照.md」到"\进度快照"文件夹。
- 文件名格式：`序号-简要描述.md`（序号纯递增，不回改旧文件名）。
- 快照内容包含：已完成内容、当前修改到哪个模块、待办清单（未完成）、下一步要做什么。
- 开始任务前，只需读取最新1份快照，不读取全部历史快照。

## 三、续写规则
- 记住约定的：格式、风格、结构、命名规范、技术栈。
- 不重复已完成的内容，只从上一次结束的位置继续。
- 修改时只输出改动部分或完整新版，提前说明是哪种。

## 四、中断恢复规则
- 如果新开对话、刷新页面、更换设备，自动读取上一轮最新的「进度快照」。
- 接到后，直接从上一轮结束的位置继续，不再重复提问确认。

## 五、版本编译
- 版本修改完成后自动编译新版本到"\dist"文件夹。
- 以版本号名称创建文件夹保存新版本，版本号自动递增。
- 不删除旧版本的文件夹。

## 六、源码备份
- 每个功能阶段（非每次小改动）开始前，自动备份当前源代码到"\backup"文件夹。
- 自动分析backup文件夹里已有的备份版本号，从最新版本号递增。
- 同一阶段内的多次小修改不重复备份。
- 每次创建阶段备份前，先执行一次 GitHub 检查点推送，不跳过。
- 备份前推送统一执行：`bash tool/pre_backup_push.sh "chore: pre-backup checkpoint - 阶段说明"`
- 本地 `backup/` 目录继续作为离线兜底备份保留，不要求把 `backup/` 目录本身提交到 GitHub。
- 但每次“开始备份这个动作”必须先把当前可回滚版本推送到 GitHub；备份完成后如有新增规则、快照或阶段记录，也要再次推送到 GitHub。

## 七、环境配置
- 代理地址：192.168.0.211:7890（遇到网络问题时使用）
- Flutter：G:\data\flutter
- FFmpeg：G:\data\app\DIT\ffmpeg

## 八、GitHub同步与回滚规则
- 远端仓库固定为：`https://github.com/luojiang419/srt-sync.git`
- 当前这套环境如同时存在 WSL/Linux Git 与 Windows Git，GitHub 推送优先使用 Windows 侧 Git 凭据链路，避免 Linux 侧缺少 `credential-manager` 导致 push 失败。
- 统一通过 `tool/pre_change_push.sh` 执行“修改前检查点提交 + 推送”；该脚本需优先复用 Windows 已登录的 GitHub 凭据。
- 统一通过 `tool/pre_backup_push.sh` 执行“备份前检查点提交 + 推送”。
- 推荐推送方法：
  1. 日常修改前推送：`bash tool/pre_change_push.sh "chore: pre-change checkpoint - 本次修改说明"`
  2. 阶段备份前推送：`bash tool/pre_backup_push.sh "chore: pre-backup checkpoint - 阶段说明"`
  3. 如需手动直推，优先使用 Windows 侧 Git：`cmd.exe /c git push origin main`
- 从现在开始，每次进入新的修改任务前，先执行一次 GitHub 同步，不跳过。
- 标准流程：
  1. 先检查 Git 状态，确认当前工作区可提交。
  2. 执行 `tool/pre_change_push.sh "chore: pre-change checkpoint - 本次修改说明"`。
  3. 只有在 push 成功后，才开始新的代码或文档修改。
  4. 如果 push 失败（认证失败、网络失败、远端不存在、分支冲突），先暂停修改，优先解决 GitHub 同步问题。
- 阶段备份标准流程：
  1. 先执行 `bash tool/pre_backup_push.sh "chore: pre-backup checkpoint - 阶段说明"`。
  2. 确认 GitHub 推送成功后，再创建新的 `backup/vX.Y.Z` 目录。
  3. 备份完成后继续本阶段修改。
  4. 如备份阶段又新增规则文档、进度快照、脚本或流程文件，完成后再次提交并推送到 GitHub。
- 每次功能完成后，除保留 `backup` 和 `进度快照` 外，还要把本次功能提交并推送到 GitHub。
- 需要回滚时，优先使用 GitHub 提交历史回滚；`backup` 目录作为本地兜底版本。
- 新开对话或中断恢复时，除读取最新进度快照外，也要先检查最近一次 GitHub 提交是否已经同步。

现在请你：
1）理解这套规则；
2）告诉我你已准备好；
3）我再把具体任务发给你。
